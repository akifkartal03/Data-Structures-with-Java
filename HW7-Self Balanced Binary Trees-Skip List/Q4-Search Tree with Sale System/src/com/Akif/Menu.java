package com.Akif;
/**
 * Since We have more than one Menu in our Program.
 * We created a Menu interface to show these menus by using polymorphism.
 * */
public interface Menu {
    void showMenu();
}
